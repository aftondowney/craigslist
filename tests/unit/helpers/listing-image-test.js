import { listingImage } from 'craigs-list/helpers/listing-image';
import { module, test } from 'qunit';

module('Unit | Helper | listing image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingImage([42]);
  assert.ok(result);
});
