{{#if addNewCategory}}
<h1>New Category</h1>
<div>
  <form>
   <div class="form-group">
     <label for="category">Category</label>
     {{input value=category id="category"}}
   </div>

   <button {{action 'saveCategory'}}>Save</button>
 </form>
</div>
{{else}}
<button class= "btn btn-xs"{{action 'categoryFormShow'}}>New Category</button>
{{/if}}
